<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="presenter"
            type="com.pasich.mynotes.ui.presenter.NotePresenter" />

        <variable
            name="activateEdit"
            type="boolean"
            android:value="false" />

    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/noteLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/colorSurface"
        tools:context=".ui.view.activity.noteEditor.NoteActivity">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <com.google.android.material.appbar.AppBarLayout
                android:id="@+id/appBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent">

                <com.google.android.material.appbar.MaterialToolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:background="@android:color/transparent">

                    <!-- Центрований контент (показується коли розгорнуто) -->
                    <LinearLayout
                        android:id="@+id/centerContent"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_horizontal"
                        android:gravity="center_horizontal"
                        android:orientation="vertical">

                        <LinearLayout
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:gravity="center_horizontal"
                            android:orientation="horizontal">

                            <TextView
                                android:id="@+id/title_toolbar_tag_center"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:gravity="center"
                                android:textAlignment="center"
                                android:textColor="?attr/colorPrimary"
                                android:textSize="12sp"
                                android:visibility="gone"
                                tools:text="tag_name"
                                tools:visibility="visible"/>

                            <TextView
                                android:id="@+id/duplicateTag"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:layout_gravity="center_horizontal"
                                android:fontFamily="@font/railway_medium"
                                android:textColor="?attr/colorPrimary"
                                android:textSize="12sp"
                                android:visibility="gone" />
                        </LinearLayout>

                        <TextView
                            android:id="@+id/title_toolbar_data_center"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:gravity="center"
                            android:textAlignment="center"
                            android:textSize="12sp"
                            tools:text="last edit data_name" />

                        <TextView
                            android:id="@+id/wordCountCenter"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:gravity="center"
                            android:textAlignment="center"
                            android:textColor="?attr/colorOnSurfaceVariant"
                            android:textSize="11sp"
                            android:visibility="gone"
                            tools:text="42 words" />
                    </LinearLayout>

                    <!-- Вміст зліва (показується коли згорнуто) -->
                    <LinearLayout
                        android:id="@+id/endContent"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="start|center_vertical"
                        android:layout_marginEnd="16dp"
                        android:orientation="vertical"
                        android:visibility="gone">

                        <TextView
                            android:id="@+id/title_toolbar_collapsed"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="start"
                            android:ellipsize="end"
                            android:maxLines="1"
                            android:textColor="?attr/colorOnSurface"
                            android:textSize="11sp"
                            android:textStyle="bold"
                            tools:text="Note Title" />

                        <LinearLayout
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_gravity="end"
                            android:orientation="horizontal">

                            <TextView
                                android:id="@+id/title_toolbar_data_collapsed"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:textColor="?attr/colorOnSurfaceVariant"
                                android:textSize="11sp"
                                tools:text="date" />

                            <TextView
                                android:id="@+id/title_toolbar_tag_collapsed"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:layout_marginStart="5dp"
                                android:layout_marginEnd="8dp"
                                android:textColor="?attr/colorPrimary"
                                android:textSize="11sp"
                                android:visibility="gone"
                                tools:text="#tag" />


                        </LinearLayout>
                    </LinearLayout>

                    <!-- Індикатор прогресу скролу -->
                    <ProgressBar
                        android:id="@+id/scrollProgressIndicator"
                        style="?android:attr/progressBarStyleHorizontal"
                        android:layout_width="30dp"
                        android:layout_height="10dp"
                        android:layout_gravity="end|center_vertical"
                        android:layout_marginEnd="5dp"
                        android:max="100"
                        android:progress="0"
                        android:progressDrawable="@drawable/scroll_progress_gradient"
                        android:visibility="gone" />

                </com.google.android.material.appbar.MaterialToolbar>
            </com.google.android.material.appbar.AppBarLayout>


            <androidx.core.widget.NestedScrollView
                android:id="@+id/scrollView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:fillViewport="true"
                android:fitsSystemWindows="false"
                android:scrollbarSize="0dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <EditText
                        android:id="@+id/notesTitle"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="10dp"
                        android:autoLink="all"
                        android:autofillHints="no"
                        android:background="@null"
                        android:enabled="@{activateEdit, default=false}"
                        android:fontFamily="@font/railway_medium"
                        android:hint="@string/noteTitle"
                        android:inputType="textMultiLine"
                        android:linksClickable="true"
                        android:maxLength="@integer/maxLengthNotesTitle"
                        android:maxLines="@integer/countLinesNoteTitle"
                        android:paddingStart="20dp"
                        android:paddingEnd="20dp"
                        android:textColor="@drawable/selector_color_title"
                        android:textStyle="bold"

                        tools:ignore="TouchTargetSizeCheck" />


                    <EditText
                        android:id="@+id/valueNote"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:autoLink="web|email"
                        android:autofillHints="no"
                        android:background="#00FFFFFF"
                        android:enabled="@{activateEdit, default=false}"
                        android:focusable="@{activateEdit, default=false}"
                        android:focusableInTouchMode="@{activateEdit, default=false}"
                        android:gravity="start|top"
                        android:hint="@string/hintTextNote"
                        android:inputType="textAutoComplete|text|textCapSentences|textMultiLine"
                        android:longClickable="true"
                        android:maxLength="@integer/maxLengthNotesTextValue"
                        android:minHeight="100dp"
                        android:padding="20dp"
                        android:singleLine="false"
                        android:textColor="@drawable/selector_color_value" />

                </LinearLayout>
            </androidx.core.widget.NestedScrollView>

        </LinearLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/editActive"
            style="@style/fab_custom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="25dp"
            android:layout_marginBottom="25dp"
            android:contentDescription="@string/editNoteButton"
            android:onClick="@{(view) -> presenter.activateEditNote()}"
            android:visibility="@{activateEdit ? View.GONE : View.VISIBLE}"
            app:layout_dodgeInsetEdges="bottom"
            app:srcCompat="@drawable/ic_edit" />


    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>