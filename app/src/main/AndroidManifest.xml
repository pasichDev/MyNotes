<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />

            <data android:scheme="*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />

            <data android:scheme="*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />

            <data android:scheme="*" />
        </intent>

        <package android:name="com.google.android.apps.translate" />

        <!-- Read by PlayServicesAvailability. Without this the package table reports Play
             services as absent on API 30+, and sync would hide itself on every device. -->
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:name=".MyApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/DefaultTheme"
        tools:targetApi="tiramisu">
        <activity
            android:name=".ui.view.activity.HelpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.BackupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.AboutActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.SupportActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.ChangelogActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.LibsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.PhotoViewActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RoutingActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

        </activity>
        <activity
            android:name=".ui.view.activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
        <!--
            No intent filter: every launch site uses an explicit Intent
            (NavigationController, SettingsFragment), so the ACTION_VIEW filter matched nothing
            but the app's own implicit VIEW intents for web links, which Android 14 refuses to
            deliver to a non-exported component.
        -->
        <activity
            android:name=".ui.view.activity.TrashActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.TagsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.noteEditor.NoteActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.view.activity.noteEditor.NoteExtendedEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.view.activity.ShareActivity"
            android:exported="true"
            android:label="@string/NewNote">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter android:label="@string/save_to_notes">
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.view.activity.SnoozeActivity"
            android:theme="@style/Theme.Translucent"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.ReminderTapActivity"
            android:theme="@style/Theme.Translucent"
            android:exported="false" />
        <activity
            android:name=".ui.view.activity.TasksActivity"
            android:exported="false" />
        <receiver
            android:name=".ui.receiver.ReminderReceiver"
            android:exported="false" />
        <receiver
            android:name=".ui.receiver.TaskReminderReceiver"
            android:exported="false" />

        <service
            android:name=".ui.tile.NoteQuickTile"
            android:exported="true"
            android:icon="@drawable/ic_add"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".ui.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
